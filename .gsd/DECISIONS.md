# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 | M001 | arch | Version 1 deployment model | Local-first web app on the home network | Easier to develop and keep live, and matches the user’s intended environment | Yes — when M002 public deployment begins | agent |
| D002 | M001 | pattern | Physical fridge/freezer identification | Printable QR code per storage context | Scanning the code should immediately recognize the fridge/freezer and connect the user to it with the right context | No | agent |
| D003 | M001 | pattern | Inventory intake trust model | Review-first draft confirmation | Photo/AI capture is helpful but not trusted enough to become truth without human confirmation and correction | No | agent |
| D004 | M001 | data | Quantity model for version 1 | Presence-first item tracking | The first version should be useful without requiring precise quantity math everywhere | Yes — if later cooking flows need richer quantity detail | agent |
| D005 | M001 | data | Expiry handling for foods without printed dates | User-estimated expiry is supported alongside explicit expiry dates | Produce and similar foods still need aging awareness even when the producer provides no date | No | agent |
| D006 | M001 | pattern | Inventory maintenance model | Explicit update/remove/discard actions | Trust depends on users being able to keep inventory current after food is used | No | agent |
| D007 | M001 | scope | Public web deployment | Deferred to M002 | Local usefulness and product truth should be proven before public hosting and domain concerns are added | Yes | agent |
| D008 | M001/S01/T01 | library | Next.js router | App Router (not Pages Router) | App Router enables React Server Components for cleaner data-fetching patterns in later slices; dynamic route params are async in Next.js 15 | Yes — if Pages Router proves simpler for local-only use | agent |
| D009 | M001/S01/T01 | library | Tailwind version | Tailwind CSS v4 with @tailwindcss/postcss | v4 uses CSS-native @theme config instead of tailwind.config.ts; no separate config file needed; aligns with modern PostCSS pipeline | No — downgrade to v3 would require config restructure | agent |
| D010 | M001/S01/T01 | arch | Next.js version | 15.5.14 (not 15.2.3) | 15.2.3 had CVE-2025-66478 (critical); upgraded immediately before first install | No — never downgrade below the patched version | agent |
| D011 | M001/S01/T02 | library | SQLite client | better-sqlite3 (synchronous) | Synchronous API matches Next.js RSC model naturally — no async/await in store functions, no Promise wrapper needed. File lives at data/fridges.db | Yes — switch to libsql/Turso if remote/edge deployment is needed | agent |
| D012 | M001/S01/T02 | library | Short ID generation | nanoid(10) | URL-safe, 10-char IDs have negligible collision probability at local scale; no UUID verbosity in URLs or QR codes | No | agent |
| D013 | M001/S01/T02 | library | QR code rendering | qrcode npm package, SVG output | SVG renders in RSC (no canvas/browser API), is printable without external image requests, and colors match the app's design tokens | No | agent |
| D014 | M001/S02/T01 | library | AI extraction client | openai npm package with gpt-4o-mini vision | gpt-4o-mini balances cost and capability for grocery image understanding; structured JSON output mode avoids regex parsing; deterministic stub makes the full flow testable without a live key | Yes — swap model if gpt-4o-mini is discontinued or a cheaper/local alternative proves adequate | agent |
| D015 | M001/S02/T01 | pattern | Missing-photo error handling | try/catch around request.formData() returning 400 | Next.js throws a TypeError on empty/malformed multipart bodies before any field check can run; catch is the only reliable path for the missing-photo 400 response | No — this is a Next.js runtime behavior that must be worked around | agent |
| D016 | M001/S02/T02 | pattern | Server Action invocation style | Imperative call from handleConfirm, not via form/useActionState | Confirm payload is programmatic JSON assembled from React state — not FormData from a form submission. useActionState is designed for form-driven actions; imperative call is cleaner here | Yes — revisit if the confirm flow needs form-level progressive enhancement | agent |
| D017 | M001/S02/T02 | pattern | Draft row ID assignment | nanoid(10) assigned client-side after API response | Avoids a round-trip to the server for ID assignment; same IDs flow from review state into the DB; consistent with the existing nanoid(10) pattern from S01 | No | agent |
| D018 | M001/S03/T01 | data | How to handle the boolean expiry_estimated field across SQLite and TypeScript | Store as INTEGER 0/1 in SQLite; coerce to boolean at the store read boundary in listInventoryItems | SQLite has no native BOOLEAN type. Coercing at the read boundary keeps the DB schema simple and ensures all TypeScript consumers see a real boolean — no casting needed in components or actions. | No | agent |
| D019 | M001/S03/T01 | data | Atomicity strategy for draft-to-inventory promotion | db.transaction() wraps both INSERT into inventory_items and UPDATE intake_drafts.status='confirmed' in a single block | Partial promotion (inventory row inserted but draft not confirmed, or vice versa) would leave the system in an inconsistent state — the item would appear both as pending draft and as inventory simultaneously. A single transaction makes this impossible. | No | agent |
| D020 | M001/S03/T02 | pattern | How to handle router.refresh() after a Server Action completes in InventorySection | useTransition wraps router.refresh() after promotion so React treats the route invalidation as a non-urgent background transition | Without useTransition, router.refresh() can interrupt the success banner before the user sees it. Wrapping in startTransition lets React show the "✓ N items added" state immediately while the re-fetch happens in the background. | Yes | agent |
| D021 | M001/S04/T02 | pattern | How to handle exclusive edit mode in a per-row action UI | editingItemId: string \ | null (single shared state) with edit/discard buttons disabled when editingItemId !== null | Only one row can be meaningfully in edit mode at a time; a shared string\ | agent |
| D022 | M001/S04/T01 | pattern | Error contract for store mutation functions that target a specific row | Throw when changes===0 rather than return a boolean; setInventoryItemStatus adds WHERE status='active' guard | Returning changes===0 silently as success would hide bugs where the wrong ID was passed or the row didn't exist. Throwing surfaces the problem immediately and consistently propagates to the Server Action catch block as a structured { success: false, error } response. The active-only guard on status flips makes double-acting on an item a visible error rather than a silent no-op, preserving data integrity trust. | No — consistent error contract is a load-bearing assumption downstream | agent |
| D023 | M001/S05/T02 | pattern | Whether StatusSection should be a server or client component | Server component | The status/alerts/suggestions section is entirely read-only — it consumes pre-computed props from page.tsx and renders static HTML. There is no user interaction, no local state, and no client-side data fetching. Keeping it as a server component adds zero hydration cost and no client bundle weight. | Yes | agent |
| D024 |  | M001/S06 | Dev server network binding for household LAN access | Add --hostname 0.0.0.0 to the dev script in package.json | next dev defaults to 127.0.0.1 (localhost only), which is unreachable from other devices on the home Wi-Fi network. Adding --hostname 0.0.0.0 binds to all interfaces, enabling LAN access without requiring a production build. This is a one-word addition with no impact on tests or the production build. npm start (production mode) already binds to 0.0.0.0 by default. | No — localhost-only binding would break the local-first household use case | agent |
| D025 | M001/S06 | pattern | S06 final-assembly proof strategy | Use two-track proof: automated regression for the assembled data path plus live LAN acceptance for network reachability and QR entry | S06 must prove more than fixture-level contracts, but printed-QR scan and cross-device LAN reachability cannot be fully automated inside the repo. Combining automated regression (tests/build/curl/browser) with explicit live LAN acceptance keeps the proof honest without overclaiming. | Yes — if device-lab QR automation is added later | agent |
| D026 | M002 | arch | M002 deployment model | Docker Compose on any Linux/macOS home device | Target devices (Pi, Mac Mini, old Linux laptop) may not have Node installed. Docker makes the environment portable and reproducible. `better-sqlite3` native module compilation is handled inside the build stage — host has no Node requirement. | Yes — if a lighter wrapper becomes available | collaborative |
| D027 | M002 | library | Service worker implementation for Next.js App Router | Serwist (`@serwist/next`) | Modern fork of next-pwa/Workbox with first-class Next.js App Router support, TypeScript, and clear docs. Configured with `withSerwistInit` in next.config.ts; SW disabled in development to avoid stale cache. | Yes — if Serwist is abandoned | agent |
| D028 | M002 | arch | PWA service worker on plain HTTP LAN | Document and accept the limitation | Browsers require HTTPS or localhost to register a service worker. Plain `http://192.168.x.x:3000` LAN URLs block SW registration. Manifest and standalone install still work. Adding HTTPS (self-signed cert + local CA) is disproportionate complexity for this milestone. | Yes — if HTTPS is added in a later milestone | collaborative |
| D029 | M002 | library | mDNS advertisement library | `bonjour-service` (pure TypeScript) | No native compilation required unlike `node-mdns`. Pure JS/TS implementation built on `multicast-dns`. Requires `network_mode: host` in Docker for multicast packets to reach the LAN. | Yes — if a more robust mDNS solution is needed | agent |
| D030 | M002 | pattern | mDNS + Docker networking | `network_mode: host` for the mDNS slice; S04 removes `ports:` mapping | `network_mode: host` is incompatible with explicit `ports:` mapping in docker-compose. S04 switches the compose file to host networking, making the port implicit (3000). S01–S03 use bridge networking with explicit port mapping. | No — host networking is the only reliable option for mDNS multicast from Docker | agent |
| D031 | M002 | pattern | Last-used fridge memory implementation | `localStorage['lastFridgeId']` written on every fridge context visit; root page redirects client-side if value exists | Server-side session would require auth infrastructure. localStorage is the correct tool: per-device, zero server state, instant redirect. `start_url: "/"` in manifest ensures the redirect logic always runs on PWA launch. | Yes — if multi-device sync is ever needed | agent |
| D032 | M002 | arch | Next.js production build mode for Docker | `output: 'standalone'` in next.config.ts | Standalone output bundles only the required Node modules into `.next/standalone/`, dramatically reducing production image size. Entrypoint changes from `next start` to `node .next/standalone/server.js`. `public/` and `.next/static/` must be copied into the standalone directory manually in the Dockerfile. | No — standalone is the correct choice for containerised Next.js | agent |
| D033 | M002/S01/T02 | pattern | Which `Dockerfile` stage to copy `better-sqlite3` native modules from in multi-stage builds | `deps` stage (pre-prune), not `builder` stage | `npm prune --omit=dev` in the builder stage removes runtime-critical packages like `bindings` and `file-uri-to-path` that are needed by the `better-sqlite3` loader at process start. Copying from `deps` (which has the full unmodified `node_modules`) avoids the missing-module crash at container start. | No — the deps stage is the only reliable source for native module packages | agent |
| D034 | M002/S02/T01 | library | Icon generation tool | Sharp (devDependency) + inline SVG rasterization in `scripts/generate-icons.mjs` | Avoids Canvas/node-canvas native binary complexity at runtime. Sharp is dev-only; at runtime the container serves static PNG files — no Sharp in the production image. The script is idempotent and self-validating (exits non-zero on dimension mismatch). | Yes — if a different design tool is preferred | agent |
| D035 | M002/S02/T02 | pattern | TypeScript config for service worker source | Separate `tsconfig.worker.json`; `app/sw.ts` excluded from main `tsconfig.json` | Adding `"webworker"` to the global lib array removes DOM-only types (`navigator`, `window`, `document`, `EventTarget & HTMLInputElement.files`, etc.), breaking all component event handlers. Excluding the SW file from the main tsconfig and adding a dedicated worker tsconfig is the standard Next.js + Serwist pattern. | No — this split is required whenever a SW coexists with React components | agent |
| D036 | M002/S04/T01 | build | How to include bonjour-service in Next.js standalone output when the import is behind a runtime guard | Use outputFileTracingIncludes in next.config.ts to force nft to include the package | Next.js nft (node-file-trace) cannot statically evaluate NODE_ENV === 'production' guards inside dynamic import() calls in instrumentation.ts. Without explicit inclusion, bonjour-service is absent from .next/standalone/node_modules and the runtime call fails silently. outputFileTracingIncludes is the canonical Next.js escape hatch for this pattern. | Yes — removable if Next.js nft gains ability to evaluate runtime guards statically | agent |
